import { smoochSans } from '@/app/FONTS/fonts';
import React from 'react';

const SaveBlogTitile = () => {
    return (
        <div>
            <p className={`${smoochSans.className} text-2xl`}>Select a collection</p>
        </div>
    );
};

export default SaveBlogTitile;